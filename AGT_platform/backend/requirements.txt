flask==3.0.3
flask_cors==4.0.1
sqlalchemy==2.0.32

alembic==1.13.2
python-dotenv==1.0.1
authlib==1.3.1
requests==2.32.3
numpy>=1.26,<3
pyjwt==2.9.0

# RAG embeddings (default ``RAG_EMBEDDING_BACKEND=sentence_transformers`` in Config).
sentence-transformers>=3.0.0

celery==5.4.0
redis==5.0.8

boto3==1.35.36
openai==1.55.3
anthropic>=0.40.0

nbformat==5.10.4
pypdf==5.0.1
psycopg2-binary==2.9.11
pytest

# AWS CLI: this app uses boto3 for S3 only. AWS CLI v2 is not installable via pip as
# awscli 2.x (PyPI has awscli 1.x only). Install the standalone CLI from AWS if needed.